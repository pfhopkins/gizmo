name: C/C++ CI

on:
  push:
    branches: [ "master","autotest"]
  pull_request:
    branches: [ "master","autotest" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: configure
      run: ./configure
    - name: make
      run: make
