*.o
/GIZMO
GIZMO_config.h
compile_time_info.cc
/Config.sh
/Config.sh.*
/Makefile.systype.*
/compiler_test_stdout
/compiler_test_stderr

# Loose test/run artifacts in repo root (rsync'd-back snapshots, one-off logs)
/TREECOOL
/ewald_spc_table_*.dat
/spcool_tables/
/*_ics.hdf5
/output/
/run_*.log
/makeout
/makeerr
/makelog
/tstlog

# compile_suite artifacts
/compile_suite_errors/
/compile_suite_output.log
/compile_suite_output_*.log
/compile_suite_results.txt

# Test artifacts
test/*/output/
test/*/output_*/
test/*/GIZMO
test/*/*_ics.hdf5
test/*/spcool_tables/
test/*/spcool_tables.tgz
test/*/TREECOOL
test/*/ewald_spc_table_*.dat
test/*/glass_*.hdf5
test/*/*.params-usedvalues
test/*/test_*.out
test/*/test_*.err
test/*/__pycache__/
test/*/*.png
test/*/test_helmholtz
test/*/test_nuclear

# Binary reference/data files (large, should not be in git)
test/*/*.hdf5
*.tgz
*.tar.gz

# EOS table data files (large, downloaded separately)
eos/helmholtz/helm_table.dat

# Python build artifacts
python_src/gizmo.egg-info/
__pycache__/
*.pyc

# Build directory
build/

# Claude Code per-session state (auto-memory, transcripts, etc.)
.claude/

# Step-15 benchmarking — entire tree is testing-ancillary, kept locally
# (~/Documents/work/code/gizmo/benchmarks/step15_optimization/) but not in repo.
# Drivers, sweep configs, analysis scripts, sanity probes — none of it is part
# of the production code; meaningful only inside this immediate testing setup.
test/benchmark/step15/

# Local handoff / agent memory / planning notes. Same rationale as .claude/:
# meaningful only to the immediate working session, not part of the code.
docs/

# Stray dev-scratch test notes (out-of-date WIP memos shipped from upstream
# branches; not user-facing docs).
test/biermann_growth/NOTES.md

# Python bytecode caches (regenerated on every pytest invocation, not source).
__pycache__/
*.pyc
*.pyo
test/fire_m11i/
test/poisson_starforge/